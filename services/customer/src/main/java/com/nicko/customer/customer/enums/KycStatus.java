package com.nicko.customer.customer.enums;

/** Initial verification values; the ERD does not specify the allowed values. */
public enum KycStatus {
    NOT_STARTED,
    PENDING,
    UNDER_REVIEW,
    APPROVED,
    REJECTED,
    EXPIRED
}
