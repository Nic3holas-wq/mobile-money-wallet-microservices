package com.nicko.customer.customer.enums;

/** Initial lifecycle values; the ERD does not specify the allowed values. */
public enum CustomerStatus {
    PENDING,
    ACTIVE,
    SUSPENDED,
    CLOSED
}
